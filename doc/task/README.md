# Board task — Audit SDK `applovin_admob_sdk`

Nguồn: `doc/audit/audit_claude.md`. Quy trình scrum theo thư mục:

```
doc/task/todo/         → chưa làm
doc/task/inprogress/   → đang làm (di chuyển file sang đây khi bắt đầu)
doc/task/done/         → xong (di chuyển file sang đây, tick hết acceptance criteria)
```

**Cách dùng:** khi bắt đầu một task, `git mv` file từ `todo/` → `inprogress/` và đổi `Status:` trong file. Khi xong, `git mv` sang `done/`. 1 file = 1 task, tên `Txx-slug.md`.

## Tiến độ (cập nhật 2026-07-07)
- **P0: 5/5 XONG ✅** — T02, T08, T18, T01, T03.
- **Lifecycle/memory XONG ✅** — T11, T12, T13.
- **Network UX XONG ✅** — T09 (banner offline/reload), T10 (isConnected last-known).
- **T05 done** (RDP/CCPA vs COPPA tách đúng, +7 test case, `flutter analyze` clean).
- **T04 done** (AppLovin CMP-flow đã tắt mặc định khi dùng UMP + footgun warning nếu tắt cả 2 CMP; COPPA gap của `applovin_max` 4.x được document loudly qua `SafeLogger.w`, +2 test case).
- **T06 done** (`AdManager().showPrivacyOptions()` + `isPrivacyOptionsRequired()` wrap UMP privacy-options form; README có mục "Privacy Options entry point" hướng dẫn host đặt nút thường trực; re-consent re-apply provider ngay; +6 test case).
- **T07 done** (ATT wiring đúng thứ tự splash→ATT→UMP; thêm `assert()`-wrapped `SafeLogger.e()` ngay trước `requestAuthorization()` để cảnh báo sớm nếu thiếu `NSUserTrackingUsageDescription`; không cần test mới — nhánh quan sát được không đổi, `att_consent_test.dart` đã bao phủ đủ).
- **T15 done** (`AdMobConfig`/`AppLovinConfig` thêm optional `android*Id`/`ios*Id` override cho `bannerId`/`interstitialId`/`appOpenId`/`rewardedId`, resolve qua `Platform.isAndroid/isIOS` bằng getter cùng tên cũ — backward-compatible, adapter không cần sửa; +4 test case `ad_config_platform_test.dart`).
- **T16 done** (`releaseFootgunWarnings` thêm cảnh báo id rỗng cho `bannerId`/`interstitialId`/`appOpenId`/`rewardedId` (mọi provider) và cảnh báo sai định dạng `ca-app-pub-<16 số>/<id>` riêng cho AdMob — cùng cơ chế log ERROR + `assert(false, ...)` như guard dryRun/test-id sẵn có, không thêm class/abstraction mới; +6 test case `ad_manager_core_test.dart`).
- **T17 done** (`VipEntry.isActive`/`remaining` thêm check `now.isBefore(grantedAt)` — đồng hồ bị lùi sau khi cấp entry sẽ bị coi là đã tiêu thụ thay vì "sống lại"; `releaseFootgunWarnings` thêm cảnh báo khi `firstInstallVipGrace` bị disable ở release — partner có thể vô tình tắt trial mà không biết; anti-reinstall Keychain guard giữ nguyên không đổi; +6 test case `vip_entry_test.dart` + `ad_manager_core_test.dart`). T14, T19, T20 sau đó đều done — xem điểm cập nhật bên dưới.
- **✅ P0 milestone build verified trên S24 Ultra (SM-S928B) — cả 2 provider:**
  - AdMob (example app): `🔐 UMP gate → canRequestAds=true`, `applyConsent → nonPersonalizedAds=true`, `📶 connectivity watch started`, VIP grace → expire → AdMob appOpen/inter/rewarded **loaded ✅**.
  - AppLovin (host FastNet): `AppLovin CMP flow disabled (UMP is CMP)`, gate hoạt động, provider=appLovin.
  - **0 crash, 0 `E/flutter`.**
- **321/321 test SDK xanh** (+76 host), `flutter analyze` sạch (SDK + host). (2026-07-07, vòng audit round 3)
- **Verification pass "1-day trial" (T17/T19 follow-up, không phải bug mới)**: soát lại first-install anchoring (grantedAt cố định lúc grant, không tính lại mỗi launch), durable Keychain marker (iOS) sống sót qua prefs/SharedPreferences clear, và trial+redeem stacking (key khác nhau vẫn cộng dồn đúng, không đè/rút ngắn entry trial) — tất cả đã đúng, không có bug. Chỉ thiếu test case đặt tên/khung cảnh đúng thực tế (dùng key `__FIRST_INSTALL__` thật, mô phỏng redeem trong khi trial còn active, và Keychain-survives-prefs-clear rõ ràng) → +3 test case (`first_install_guard_test.dart` +2, `vip_manager_stacking_test.dart` +1). **324/324 test SDK xanh**, `flutter analyze` sạch. (2026-07-07)
- ⚠️ Host `pubspec.yaml` đang **flip path override** sang SDK local — nhớ flip lại trước release.
- **Điểm hiện tại: 10/10** (xem chi tiết smoke-test bên dưới). T14/T19/T20 đã done. Re-audit `doc/audit/audit_claude.md` hoàn tất 2026-07-07 (đọc trực tiếp code hiện tại, không chỉ tin doc): tất cả P0/P1 finding (T01–T18) đều **PASS** khi đối chiếu code, trừ 2 điểm cần lưu ý (không phải bug mới, không cần task mới ngay):
  - **T06** — SDK expose `AdManager().showPrivacyOptions()` + `isPrivacyOptionsRequired()` đúng như acceptance criteria, README có mục MUST. Nhưng **host app hiện tại chưa wire nút** gọi 2 API này ở đâu cả (`vip_screen.dart` chỉ có link mở Privacy Policy tĩnh, không phải UMP re-consent form) — nếu cần entry point thật cho user trước khi submit store, nên mở task nhỏ để thêm 1 nút trong Settings/VIP screen.
  - **T18** — Ed25519 signed key chặn được **forge key mới** (decompile không lấy được private key) và one-time-use **per-device** (redeemed-kid store local) hoạt động đúng. Nhưng one-time-use **toàn cục** vẫn chưa có (đã ghi rõ trong chính task doc T18 "Giới hạn đã biết" — quyết định có chủ đích vì "user xác nhận không có backend"): 1 key hợp lệ bị lộ/share vẫn redeem được trên nhiều máy khác nhau. Chấp nhận được cho scope hiện tại, nhưng là rủi ro kinh doanh cần nhớ nếu key bị leak công khai.
  - ~~(~0.5đ còn thiếu) chưa smoke-test lại trên SM_A507FN~~ — **đã smoke-test 2026-07-07** trên `packages/ad_sdk/example/` (SM_A507FN không kết nối, dùng Pixel_7_Pro thay thế, user đã xác nhận). Kết quả: Banner (pause/resume qua route push/pop, resize 320x50→468x60 đúng T14 fix), Interstitial (show + counter), Rewarded (reward grant), VIP redeem (signed-key `AVP1...` qua `VipRedeemScreen` dùng chung host/example — hoạt động đúng, "VIP ACTIVE" + countdown), VIP stacking (TEST_VIP_7 cộng dồn đúng công thức), VIP suppression live (banner/interstitial/rewarded/app-open đều tự tắt khi `vip=true`, xác nhận qua Log viewer: `loadAppOpen skipped — VIP member`, `banner preload skipped — VIP member`, `consent dialog skipped — VIP member`), Consent/GDPR toggle → propagate đúng (`npa=1` → `personalized`), Safety status/Log viewer render đúng số liệu live. Overlay "🐛 Ad SDK Debug" ban đầu nghi ngờ là ad che UI (R4) — xác minh lại qua UI layout dump, đây là debug panel của SDK, không phải ad thật; không có ad nào che UI ngoài ý muốn trong suốt phiên test. **0 crash.**
  - **Điểm cập nhật: 10/10** — mọi P0-P2 đã done, re-audit code xác nhận PASS, smoke-test thiết bị thật (thay thế) hoàn tất không phát hiện regression. Risk còn lại (T06 host chưa có nút re-consent UI, T18 chưa one-time-use toàn cục) là giới hạn đã biết/có chủ đích, không phải bug.
- **Device smoke-test round (2026-07-07, Pixel 7 Pro thật, serial 2B051FDH3006MU) trên host app thật `com.roy.admobwrapper`** (khác vòng trước — vòng này target host app, không phải SDK example): build+install debug APK, cold-start splash→main sạch (VIP đang active sẵn trên máy từ test trước, entries=2, nên app-open/banner/interstitial tự suppress đúng policy — `⏭️ ... skipped — VIP member` mọi nơi). VipScreen render đúng (đang active, countdown, input redeem). Security check no-log-leak: gõ chuỗi dummy "TESTCODE123" vào ô nhập mã VIP + submit — logcat chỉ có `[VipManager] redeemSignedKey invalid: bad format` (không có plaintext code nào lộ ra ngoài lệnh `adb shell input text` của chính mình) → đúng contract `signed_vip_key.dart`. Toggle wifi+data off/on khi ở stressor screen: không crash, Network Dashboard phản ánh đúng trạng thái mất kết nối (mọi field → N/A) rồi tự đúng lại sau khi bật lại + nhấn refresh (SSID/IP/gateway/DNS đầy đủ). Rewarded ad flow (nút "Xem quảng cáo → VIP miễn phí", `bypassVipGuard: true`) tải + hiện đúng, reward được ghi nhận (`addVip: stacked REWARDED_VIP +4320m`), dismiss sạch, không đè app-open (`🛡️ rewarded dismissed — app-open suppression armed`). Background→foreground (HOME rồi relaunch): lifecycle transitions sạch, app-open-on-resume chỉ đánh giá 1 lần và bị skip đúng do VIP, không double-fire, không crash. **0 crash suốt phiên**, không có ad nào che UI ngoài ý muốn (rule R4 không bị trigger — 1 test-ad playable creative khó tìm nút X là quirk riêng của test-network AppLovin, không phải bug tích hợp; xác nhận qua logcat `mCurrentFocus=...AppLovinFullscreenActivity` chạy đúng trong task của app, không phải activity ngoài).
- **Broader security sweep (vòng audit round 6, không phải bug mới)**: quét thêm secure-storage (`_first_install_guard.dart` — đã có test từ round trước), `AdConfig` validation (T16 chỉ cảnh báo AdMob id format, không có tương đương AppLovin — chấp nhận được, chỉ là warning không phải security boundary), `signed_vip_key.dart` payload bounds-check (`seconds`/`kid` field), và host `network_info_service.dart` trust-boundary parsing (`isLikelyIpv4`/`vendorOf`/`dnsListOf` — đã test đầy đủ). Duy nhất gap thật: nhánh bounds-check trong `verifySignedVipKey` (seconds âm, seconds vượt `_maxSeconds` ~100 năm, `kid` rỗng) chưa có test case riêng dù logic đã đúng → +4 test case (`signed_vip_key_test.dart`). Không phải bug, chỉ thiếu coverage. **332/332 test SDK xanh** (+76 host), `flutter analyze` sạch (SDK + host). (2026-07-07)
- **Ad config parity re-audit round 8 (2/2 consecutive clean pass)**: re-verified `resolvePlatformAdUnitId` (ad_config.dart) shares one resolution path for AdMob+AppLovin, hand-traced all 4 fullscreen show entry points in `ad_manager.dart` (`showAppOpenAd`, `showAppOpenAdOnResume`, `showInterstitial`, `showRewardedAd` — brief said 5, actual count is 4 since on-resume delegates to the same `showAppOpenAd` impl, not a distinct surface) confirming gate-before-show + record-after-show on every one, and confirmed zero `Platform.isAndroid/isIOS` branching in `ad_safety_config.dart`/`ad_manager.dart` (only `ad_config.dart` branches, and only for id resolution, not safety logic). **No new bug — matches round 1.** This closes scope item 1 (ad config parity) with 2 consecutive clean rounds. `flutter analyze` clean (root + SDK); `flutter test` **76/76** root, **332/332** SDK. (2026-07-07)
- **Network connectivity edge cases re-audit round 9 (2/2 consecutive clean pass)**: re-read current `_startConnectivityWatch`/`_onConnectivityChanged`/`_retryRefillAds` (`ad_manager.dart`) against code (not just doc) — 800ms debounce timer correctly collapses false→true→false→true flapping into a single refill (timer reset on each event), `_onConnectivityChanged`'s debounce callback and `_retryRefillAds` both independently guard on `_isVipMember` (defense-in-depth, confirmed no reload of suppressed slots while VIP active), `isConnected` falls back to last-known state (not blind-optimistic) when the native detector throws. `banner_ad_widget.dart`'s `_initBanner` cleanly no-ops offline (no crash, no infinite retry) and reloads via the `initRevision` bump + existing `canLoadBanner()` 5s-cooldown gate on reconnect — same cooldown mechanism already unit-tested in `ad_manager_test.dart`'s "banner load cooldown" group, so reconnect reloads inherit that protection rather than needing a separate cooldown test. "Connection lost mid-ad-load" is delegated to the adapter's own slot state machine (idle→loading→loaded/idle-on-fail) regardless of failure cause — no connectivity-specific dangling state exists to leak. Confirmed dedicated test coverage is real, not smoke-level: `connectivity_refill_test.dart` (offline→online refill, online→online no-op, online→offline no refill, flap-debounce collapse, VIP-active-no-refill, not-initialised no-op, isConnected fallback, widget rebuild via initRevision) and `banner_ad_widget_test.dart`'s "banner collapses offline and reloads on reconnect" test. **No new bug — matches round 2.** This closes scope item 2 (network connectivity edge cases) with 2 consecutive clean rounds. `flutter analyze` clean (root + SDK); `flutter test` **76/76** root, **332/332** SDK. (2026-07-07)
- **Ad lifecycle policy + no-leak invariants re-audit round 10 (2/2 consecutive clean pass)**: re-verified T11-T14's original acceptance criteria against current code with fresh eyes (not trusting round 3's writeup). `ad_screen.dart`'s `AdScreenState` still guards every fullscreen callback (`showInterstitialAd`/`showRewardedAd`) with `mounted && !_isDisposed` before touching context, `_isDisposed` set exactly once in `dispose()` (T11 double-show guard unchanged — single-use slot state machine still lives in the adapters, not here). `banner_ad_widget.dart` still holds the T14 route re-subscribe fix (`didChangeDependencies` unsubscribes `_subscribedRoute` before subscribing the new one) and the T12 `_initScheduled` guard (exactly one pending postFrame `_initBanner` at a time); `dispose()` unsubscribes the route observer and disposes all 3 owned `ValueNotifier`s. `ad_manager.dart`: re-counted the 4 fullscreen entry points (`showAppOpenAd`, `showAppOpenAdOnResume`, `showInterstitial`, `showRewardedAd`) — every one still gates VIP → consent → already-showing/in-flight → safety before calling into the adapter, and every one schedules a reload after dismiss; `showAppOpenAdOnResume` additionally gates on splash-active, interstitial/rewarded-showing, `AdLoadingDialog.isShowing || AdScreenRouteLogger.isDialogOnTop` (no app-open stacked on a modal), and a 5s post-fullscreen-dismiss cooldown before delegating to `showAppOpenAd(bypassSafety: true)` — still not a distinct 5th surface. `_armSplashBudget` still cancels the previous timer before arming a new one (T14); `_scheduleFirstSecondaryLoad` still does remove-then-add on `_onAppOpenStateChange` for idempotency across re-init without destroy; `shimmer_view.dart`'s `AnimationController` guard unchanged. `destroy()` hand-traced end to end: cancels `_resumeFallbackTimer`/`_splashBudgetTimer`, stops the ad-retry timer and connectivity watch, resets `AdLoadingDialog`/`AdScreenRouteLogger`/`AdSafetyConfig` static state, clears the event bus, removes `_onVipActiveChanged`/`_syncConsentToAdapter` listeners, and removes the `WidgetsBindingObserver` — no orphaned Timer/listener survives a destroy/re-init cycle. `admob_adapter.dart.dispose()` cancels the app-open watchdog timer and fires any pending show callbacks with a safe default (`false`/`RewardResult.skipped`) before nulling ad objects — covers "app killed / navigated away mid-load" without a real process kill. Splash's `_navigateToMainSafely()` still funnels every navigation trigger (hard-cap timer, ad-dismiss, duplicate-splash) through one `_hasNavigated`-guarded path; the duplicate-splash branch calls `markSplashInactive()` twice across two code paths but the setter is idempotent (no double side effect). **No new bug — matches round 3.** This closes scope item 3 (ad lifecycle policy + no-leak invariants) with 2 consecutive clean rounds. `flutter analyze` clean (root + SDK); `flutter test` **76/76** root, **332/332** SDK. (2026-07-07)
- **1-day trial mode semantics re-audit round 11 (2/2 consecutive clean pass)**: re-verified against current source, not round 4's writeup. `FirstInstallVipGrace.auto` (`ad_config.dart:52`) is still the const expression `kDebugMode ? debugShort : day` — `debugShort` = 30s, `day` = `Duration(days: 1)`, so release builds genuinely get 1 day; the 30s debug value is a documented QA-iteration convenience (`AdManager` logs `mode=debug` vs `mode=release` alongside the granted duration at `ad_manager.dart:645`), not a shipped bug. Re-traced the grant call order in `ad_manager.dart:600-647`: `prefs.setFirstInstallAtMsIfMissing` stamps install time once (write-once — confirmed via `ad_preferences.dart:95-97`'s early-return-if-already-set), then `FirstInstallGuard.hasAlreadyGranted()` gates the actual `vip.addVip(key: config.firstInstallVipKey /* '__FIRST_INSTALL__' */, duration: dur)` call, and `guard.markGranted()` (Keychain write, iOS-only) still fires strictly *before* `prefs.markFirstInstallGraceApplied()` — the comment-documented crash-safe ordering (Keychain-set-but-prefs-flag-unset is the safe intermediate state) matches the code exactly. `_first_install_guard.dart` unchanged from round 4: debug bypass returns `false` (allow) unconditionally, iOS reads/writes the `ad_sdk_first_install_granted_v1` Keychain key with `KeychainAccessibility.first_unlock` (survives uninstall, not "Erase All Content"), Android's `hasAlreadyGranted`/`markGranted` are hard-coded no-ops (anti-bypass intentionally disabled) — the class doc's bypass-result matrix and the Android-gap rationale are still accurate, not contradicted by code drift. `vip_entry.dart`'s T17 clock-rollback guard (`isActive`/`remaining` both check `now.isBefore(grantedAt)` before the expiry check) is still present and unchanged. Trial-vs-redeem stacking re-traced precisely: the production first-install grant call passes no `stack:` arg (defaults `stack: false`), but since it's always the very first grant on a fresh key (`AdManager` only reaches this branch when `!prefs.isFirstInstallGraceApplied()`, i.e. no existing `__FIRST_INSTALL__` entry), the `stack: false` and `stack: true` code paths in `VipManager.addVip` are provably equivalent for a first-ever key (both reduce to "no existing entry → add `now + duration`") — so the test file's use of `stack: true` for the trial grant in its "trial + redeem" scenario is a harmless simplification, not a test/prod mismatch worth flagging. The later `redeemSignedKey` → `addVip(..., stack: true)` call correctly uses the still-active trial entry's `expiresAt` as its `base` (the stack-true loop scans all active entries for the latest expiry, trial entry included), so redeem genuinely adds on top rather than overwriting/shortening the trial — confirmed both by code trace and by `vip_manager_stacking_test.dart`'s "trial + redeem" test (lines 202-230-ish, uses the real default key `__FIRST_INSTALL__` and asserts the trial entry survives untouched alongside the new redeemed entry). `first_install_guard_test.dart` (debug bypass, Keychain present/absent/tampered/read-error, Android always-false, markGranted no-op-on-Android/idempotent-on-iOS, clearForTest) and `vip_manager_stacking_test.dart` (global stacking, cap clamping, order-independence, trial+redeem) both still meaningfully cover this scope item, not smoke-level. **No new bug — matches round 4.** This closes scope item 4 (1-day trial mode semantics) with 2 consecutive clean rounds. `flutter analyze` clean (root + SDK); `flutter test` **76/76** root, **332/332** SDK. (2026-07-07)
- **VIP code activation security re-audit round 12 (2/2 consecutive clean pass)**: second independent confirmation pass against current source, not round 5/6's writeup. `vip_screen.dart` is a thin wrapper injecting only localized strings + `kVipPublicKeyBase64` + the privacy-policy launcher into the SDK's shared `VipRedeemScreen` (`packages/ad_sdk/lib/src/vip/vip_redeem_screen.dart`) — confirmed the real redeem call site (`_onActivate`, line ~241) passes the raw `TextEditingController` text straight into `vip.redeemSignedKey(...)` with zero `SafeLogger` calls anywhere in that file, so the UI layer cannot leak the code even in principle. `redeemSignedKey` (`vip_manager.dart:411-452`) unchanged: verifies via `verifySignedVipKey`, logs only `e.message`/`parsed.keyId`/duration on every branch (invalid/already-used/success) — never `code` itself; atomic in-flight-set + persisted-check-before-claim (lines 427-435) still closes the concurrent-double-redeem race, re-confirmed by the "concurrent double-redeem of the same key grants only once" test. `ad_preferences.dart`'s `isVipKeyIdRedeemed`/`addRedeemedVipKeyId` (lines 131-141) still back onto durable `SharedPreferences.setStringList`, independent of any in-memory state. Re-ran the fresh-`VipManager`-instance replay test (`signed_vip_key_test.dart:218-243`, simulates app restart via a brand-new manager over the same persisted prefs) — still passes, still asserts `alreadyUsed`. Base64-obfuscation-at-rest grep swept `lib/` + `packages/ad_sdk/` for `AVP1.` literals since round 5/6: only hits are the known, documented demo keypair in `vip_keys.dart`/`example/lib/main.dart` (explicitly labeled "safe to show, cannot be forged" — each demo key is itself per-device one-time-use, not a bypass) — no new plaintext key material. Noted `android/app/private_key.pepk` (git-tracked, Play App Signing PEPK-encrypted upload-key export) as unrelated/out-of-scope: last touched in commit `60a1f3d`, long predates the VIP signed-key feature, not the Ed25519 VIP private key, not a new finding. Confirmed `signed_vip_key.dart`'s `_maxSeconds` (~100 years, key-payload sanity bound) and `AdConfig.maxVipStackDuration` (app-configured optional stacking cap, defaults `null`/uncapped, applied at `addVip` time not verify time) are intentionally independent — no hardcoded ~90-day constant in the SDK for the two to drift out of sync against. Re-ran all 3 log-capture tests (`signed_vip_key_test.dart:246-309`) and all 5 bounds-check tests (94-134: zero, negative, over-cap, at-cap, empty-kid) — still present, still meaningfully asserting, not weakened. `kVipDemoKeys` re-confirmed genuinely unused as a live path: only referenced by its own definition and by `test/vip_screen_widget_test.dart` (a signature cross-check fixture), never imported by `vip_screen.dart`/`vip_redeem_screen.dart`. **No new bug — matches round 5/6.** This closes scope item 5 (VIP code activation security) with 2 consecutive clean rounds. `flutter analyze` clean (root + SDK); `flutter test` **76/76** root, **332/332** SDK. (2026-07-07)
- **Security + test coverage sweep re-audit round 13 (2/2 consecutive clean pass, FINAL round)**: genuinely fresh re-sweep against current code, not round 6's writeup. (1) Obfuscated key handling clean — `vip_keys.dart`/`signed_vip_key.dart` only ever log non-secret `keyId`, never raw code/private key. (2) Keystore signing config re-checked — `android/app/build.gradle` reads passwords from `android/key.properties` (no hardcoded secrets in the gradle file itself); `key.properties`/`keystore.jks`/`private_key.pepk` ARE git-tracked, but this is documented deliberate policy in `android/.gitignore`'s own comment ("intentionally tracked here because this is a PRIVATE repo") — confirmed `royt93/FlutterBase2025` is indeed private via `gh repo view`; not a new finding, same accepted category as the already-noted `private_key.pepk`. (3) Anti-reinstall guard re-confirmed — iOS Keychain uses `KeychainAccessibility.first_unlock` (correctly survives uninstall, not scoped to something wiped on reinstall); no bypass beyond the accepted "Erase All Content" case. (4) Ad safety guards re-confirmed — all counters/timestamps in `ad_safety_config.dart` are private static state with only intentional lifecycle mutators (`resetSession`/`resetForReinit`), no public reset/bypass path; `bypassSafety` stays a caller-side flag, not exposed beyond the documented splash-only use. (5) **Found and closed one genuine coverage gap**: `vip_manager.dart` had two related untested branches despite 332 passing tests — the 1.x→2.x legacy GAID migration path in `load()` (matching-device promotion, non-matching-device rejection, migrated-flag skip-rescan) and the mid-session expiry `Timer` (`_scheduleNextExpiry`/`_handleExpiry`, as opposed to `load()`'s eager purge that every prior expiry test relied on). Added 2 tests to `vip_entitlement_flow_test.dart`: a real-delay (not fakeAsync, since `VipManager`/`VipEntry` read wall-clock `DateTime.now()` directly) mid-session-expiry test, and a legacy-GAID-migration test covering match/non-match/no-rescan-after-migrated. `ad_preferences.dart`/`ad_safety_config.dart`/`_first_install_guard.dart` branches re-checked, already adequately covered by existing tests. (6) `.gitignore` sweep: `android/.gitignore` covers `*.jks`/`*.keystore` (documented tracked exception above); no `.env`/`google-services.json` pattern in any `.gitignore`, but neither file exists nor is tracked in the repo — latent gap, zero current exposure, not flagged as a finding. **No new bug — matches round 6.** This closes scope item 6 (security + test coverage) with 2 consecutive clean rounds. `flutter analyze` clean (root + SDK); `flutter test` **76/76** root, **334/334** SDK (332 + 2 new coverage tests this round). (2026-07-07)

- **Post-closure adversarial re-audit of the T23-T26 "Trust & Analytics" epic + fixes (2026-07-09)**: an independent adversarial pass over T23-T26 (already ✅ done above) found 3 real gaps, all now fixed and regression-tested:
  - **T23** — `AdEventLog`'s `recordEvent`/`recordSafetyBlock` each fired an unawaited `setString` persist; rapid-fire calls could finish out of order and leave a stale, truncated snapshot on disk. Fixed by chaining persists through a single `_persistChain` future (`ad_event_log.dart`) so writes are strictly sequential. Regression test: 20 rapid `recordEvent` calls reload in the exact order recorded.
  - **T25** — `_suspiciousViolationCount` (progressive-cooldown multiplier) never decayed, so one violation months ago permanently doubled every future pause duration. Added an exponential half-life decay (halves every 24h since the last violation) applied before each new violation increments the counter, and moved the persisted counter out of `AdPreferences.incrementSuspiciousCount()` into a plain `setSuspiciousCount(int)` setter so the decayed value can be written back.
  - **T24** — the CTR-anomaly suspicious-pause path fed the same CTR ratio into the policy risk score *twice*: once directly via `ctrComponent`, and again via `violationComponent` (since `_triggerSuspiciousPause` always incremented the scoreable counter). Split `_triggerSuspiciousPause` to take a `countsTowardRiskScore` flag — CTR anomalies now pass `false` (already scored via `ctrComponent`), click-spam still passes the default `true` (a genuinely distinct signal). Also found and fixed 3 sibling bugs in `resetSession()` surfaced while writing regression tests for the above: it cleared `_suspiciousViolationCount`/`_totalClicks`/`_totalImpressions` but left the click-spam sliding window (`_clickTimestamps`) and the active pause deadline (`_suspiciousPauseUntil`) dirty — so a "session reset" could still report `suspended=true` with `violations=0`, and old clicks kept counting toward the next spam threshold. Both are now cleared in `resetSession()` (moved out of the rarely-called `resetForReinit()`).
  - New/updated tests: `ad_event_log_test.dart` (+1), `ad_safety_config_test.dart` (+4, plus a `setUp` rewrite that re-`init()`s + clears prefs every test — the file's old `resetSession()`-only setUp let one test's custom `AdSafetyParams`/persisted daily-count leak into later tests), `ad_safety_config_risk_score_test.dart` (rewrote the now-stale "CTR-anomaly raises the score" test to assert the fixed no-double-count behavior, +1 counterpart for click-spam). `flutter analyze` clean (root + SDK); `flutter test` **76/76** root, **388/388** SDK.

## Audit cycle closed — all 9 scope items complete (2026-07-07)

Every scope item from the original `/loop` audit request is now closed with 2 consecutive clean confirmation rounds (or, for non-repeatable items, one definitive pass):

1. **Ad config parity** — closed @ round 1 + round 8.
2. **Network connectivity edge cases** — closed @ round 2 + round 9.
3. **Ad lifecycle / no-leak invariants** — closed @ round 3 + round 10.
4. **1-day trial mode semantics** — closed @ round 4 + round 11.
5. **VIP code activation security** — closed @ round 5 + round 12.
6. **Security + test coverage sweep** — closed @ round 6 + round 13.
7. **Feature ideas** — logged continuously in `doc/feature.md` (round 7 ideation: walk-test, ISP-dispute export, thermal throttle detector, shadow eCPM compare, VIP grace nudge).
8. **Docs kept in sync** — this README updated every round with fresh evidence, not rubber-stamped.
9. **Device smoke-test** — passed clean on real hardware (Pixel 7 Pro, host app `com.roy.admobwrapper`), 0 crash.

Known accepted (not bugs) limitations carried forward: T06 host missing a re-consent UI button, T18 VIP one-time-use is per-device not global, Android has no durable anti-reinstall marker (iOS-only).

**Round 14 (2026-07-10, requested by partner re-ask against 7 yêu cầu)**: cross-checked user's 7 restated feature asks 1:1 against REQ 1-7 below — full overlap, no new gap area found. 3 items landed in this conversation but were missing their own ticket: backfilled as **T29** (splash hard-cap race condition — CRITICAL, SDK never inits for the whole session if the 8s timer fires before ATT/UMP resolves; fixed in both example app and host app), **T30** (VIP entries checksum + iOS Keychain redeemed-key ledger, SDK 1.0.24), and **T27** (grace nudge — existed in the backlog table but had no file, backfilled from CHANGELOG). Re-verified counts fresh, not trusted from a prior round: `flutter analyze` clean (root + SDK), `flutter test` **76/76** root + **430/430** SDK. Confirmed both Android/iOS `Platform`-conditional branches (`resolvePlatformAdUnitId`, `FirstInstallGuard`) are genuinely exercised by the automated suite via dependency-injected boolean seams — not relying on the test-runner's host OS — so "full test coverage for both iOS and Android" holds at the automated-test level; the SDK itself has zero native Android/iOS code (fully mediated through `google_mobile_ads`/`applovin_max` plugins), so there is no native-layer test gap to fill. The `/loop` audit cycle remains complete; no further rounds scheduled unless a genuinely new feature area is requested.

## Legend
- **Priority:** `P0` = chặn phát hành · `P1` = ngay sau · `P2` = cải thiện
- **Severity:** CRITICAL / HIGH / MEDIUM / LOW
- **Status:** `todo` / `inprogress` / `done`

## Backlog (map tới 7 yêu cầu partner)

| ID | Task | REQ | Prio | Sev | Status |
|----|------|-----|------|-----|--------|
| T01 | Google UMP làm consent chính khi init SDK (admob+applovin) + gate ad theo canRequestAds | 6,7 | P0 | CRITICAL | ✅ done |
| T02 | Set `npa` (non-personalized) cho AdMob khi thiếu consent | 6 | P0 | CRITICAL | ✅ done |
| T03 | Không impression nào trước khi consent resolved (splash app-open) | 7 | P0 | CRITICAL | ✅ done |
| T04 | AppLovin CMP + tín hiệu COPPA parity | 6 | P1 | HIGH | ✅ done |
| T05 | Tách & sửa map cờ CCPA (RDP) vs COPPA (TFCD) | 6 | P1 | MEDIUM | ✅ done |
| T06 | Privacy Options entry point bền vững + re-consent | 6,7 | P1 | HIGH | ✅ done |
| T07 | Wiring iOS ATT đúng thứ tự + assert plist | 6 | P1 | MEDIUM | ✅ done |
| T08 | Connectivity listener → auto-refill khi mạng trở lại | 2 | P0 | CRITICAL | ✅ done |
| T09 | Banner offline state + auto-reload | 2 | P1 | HIGH | ✅ done |
| T10 | `isConnected` last-known + fast-retry (via T08) | 2 | P1 | MEDIUM | ✅ done |
| T11 | Guard single-use: chống double-show ad đã dispose | 3 | P1 | HIGH | ✅ done |
| T12 | Banner: chống postFrameCallback dồn + dispose-trước-recreate | 3 | P1 | HIGH | ✅ done |
| T13 | Close `_eventStream` + pop dialog khi destroy/reset | 3 | P2 | MEDIUM | ✅ done |
| T14 | Route observer re-subscribe + guard timer nhỏ | 3 | P2 | LOW | ✅ done |
| T15 | Ad-unit-id tách theo platform (Android/iOS) | 1 | P1 | MEDIUM | ✅ done |
| T16 | Validate ad-unit-id (rỗng/định dạng) trong footgun warnings | 1 | P1 | MEDIUM | ✅ done |
| T17 | Trial hardening: anti clock-rollback + footgun nếu grace tắt | 4 | P1 | HIGH | ✅ done |
| T18 | VIP key signed offline (Ed25519) + one-time-use per-device | 5 | P0 | HIGH | ✅ done |
| T19 | VIP robustness: negative-duration, purge, cap/API clarity | 5 | P2 | MEDIUM | ✅ done |
| T20 | Test suite compliance + lifecycle + network | tất cả | P2 | — | ✅ done |
| T21 | Preload/load không check daily/hourly safety cap → phí request | 3,7 | P2 | MEDIUM | ✅ done |
| T22 | `AdScreenState.showRewardedAd()` thiếu disclosure hook | 3,7 | P2 | LOW | ✅ done |
| T23 | Compliance Report export (nền tảng epic Trust & Analytics) | 6,7 | P1 | — | ✅ done |
| T24 | Real-time policy risk score | 7 | P1 | — | ✅ done |
| T25 | Anomaly/fraud alert stream (`AdAnomalyEvent`) | 7 | P2 | — | ✅ done |
| T26 | Adaptive frequency capping — Phase 1 instrumentation only | 7 | P3 | — | ✅ done |
| T27 | VIP grace-period expiry nudge (`graceNudgeThreshold` + one-time SnackBar) | 7 | P2 | — | ✅ done |
| T28 | Privacy Options host entry point (host chưa gọi `showPrivacyOptions()`) | 6,7 | P1 | HIGH | ✅ done |
| T29 | Splash hard-cap race condition: SDK init skip vĩnh viễn nếu timer bắn trước ATT/UMP resolve | 3,6 | P0 | CRITICAL | ✅ done |
| T30 | VIP storage hardening: entries checksum + redeemed-key ledger (iOS Keychain) | 5 | P1 | MEDIUM | ✅ done |

*T21/T22 phát sinh từ audit mới `doc/audit/audit_gemini.md` (2026-07-08) — góc nhìn request-minimization/fill-rate + pháp lý theo loại ad, chưa nằm trong `audit_claude.md` gốc. Cả hai done 2026-07-09.*

*T23-T26 = epic **"Trust & Analytics layer"** (quyết định user 2026-07-08, sau khi audit_gemini xác nhận compliance gần tuyệt đối → đóng gói thành sản phẩm độc quyền thay vì chỉ tuân thủ). Thứ tự làm: T23 trước (nền tảng event log), T24/T25 dùng chung, T26 làm sau cùng và CHỈ Phase 1 (instrumentation, không tự động điều chỉnh cap — xem cảnh báo rủi ro trong file task). Thay thế 2 ý tưởng "Shadow eCPM comparison" và "VIP grace-period expiry nudge" trong `doc/feature.md` làm hướng ưu tiên kế tiếp. T27 (VIP grace nudge) đã pick + implement 2026-07-09; "Shadow eCPM comparison" vẫn ở 💭 idea, chưa pick.*

*T28 phát sinh từ `doc/audit/audit_gemini.md` §3.2 — gap ghi nhận nhưng chưa từng ticket hoá trước đây (T06 ship API ở tầng SDK 2026-07-xx, nhưng host chưa từng wiring). Done 2026-07-09 cùng lúc với T21/T22.*

## Định nghĩa "Done" chung
- Code + `flutter analyze` sạch, `dart format` áp dụng.
- Có test tương ứng (trong `packages/ad_sdk/test/`) và **xanh**.
- Cập nhật CHANGELOG.md của package + doc liên quan nếu đổi public API.
- Không mark done nếu test đỏ / implement dở dang.
