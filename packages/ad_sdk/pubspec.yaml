name: ad_sdk
description: Dual-provider ad SDK for Flutter (AdMob + AppLovin MAX) with built-in safety throttle, VIP bypass, and RouteAware banner lifecycle management.
version: 1.0.0
homepage: https://github.com/royt93/FlutterBase2025
repository: https://github.com/royt93/FlutterBase2025
issue_tracker: https://github.com/royt93/FlutterBase2025/issues


environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^6.0.0
  applovin_max: ^4.6.3
  shared_preferences: ^2.5.0
  advertising_id: ^2.7.1
  connection_notifier: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  # This package does not use flutter_lints or other Flutter artifacts.
