<?xml version="1.0" encoding="utf-8"?>
<!--
Auto Backup + Device Transfer rules (Android 12+, API 31+).

Required for `applovin_admob_sdk` v1.0.17+ anti-uninstall-bypass guard:
`prefs.isFirstInstallGraceApplied()` flag (in `FlutterSharedPreferences.xml`)
must be cloud-backed up so a reinstall on the same Google account restores
the flag and the first-install grace guard correctly refuses to re-grant.

`FlutterSecureStorage.xml` is NOT backed up: because we declare explicit
<include> domains, every other file in `sharedpref` (including
EncryptedSharedPreferences ciphertext) is excluded by default. Its ciphertext
is unrecoverable without the device-bound Keystore key (not part of any
backup).
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="sharedpref" path="FlutterSharedPreferences.xml" />
    </cloud-backup>
    <device-transfer>
        <include domain="sharedpref" path="FlutterSharedPreferences.xml" />
    </device-transfer>
</data-extraction-rules>
