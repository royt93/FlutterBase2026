# Copy this file to keystore.local.properties (gitignored) and set the path
# to your local clone of the private royt93/myKeyStore repo.
# Without this file, release builds fall back to debug signing.
myKeyStoreDir=/absolute/path/to/myKeyStore
